import { createAction } from 'redux-act';

export const openModal = createAction('ui/modal/observations-within-box/open');
export const closeModal = createAction('ui/modal/observations-within-box/close')