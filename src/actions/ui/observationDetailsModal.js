import { createAction } from 'redux-act';

export const openObservationDetailsModalAction = createAction('observation-details/open');
export const closeObservationDetailsModalAction = createAction('observation-details/close');
