import { createAction } from 'redux-act';

export const openObservationDetailsPopoverAction = createAction('observation-details/open');
export const closeObservationDetailsPopoverAction = createAction('observation-details/close');
