export const isObservationCreationModalOpen = state => state['observationCreationModal'].data.isOpen;
export const getCurrentStep = state => state['observationCreationModal'].data.currentStep;
export const getType = state => state['observationCreationModal'].data.type;
export const getCulture = state => state['observationCreationModal'].data.culture;
